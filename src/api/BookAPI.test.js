import BookApi from './BooksAPI'

describe('BookApi', () => {
  it('should be defined', () => {
    expect(BookApi).toBeDefined()
    expect(BookApi).toBeInstanceOf(Object)
  })

  /* TODO: Complete the API test suite */
})